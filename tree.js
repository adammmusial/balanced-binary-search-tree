import Node from "./node";

export default class Tree {
    constructor(array){
        this.buildTree(arr)
    }

    buildTree(){}

    sortArray(array){
        sortedArray = array.sort(sort((a, b) => a - b))
        return sortedArray
    }
}